<?xml version="1.0" encoding="UTF-8"?>
<SharedFlowBundle name="SF-SAML-Extractor">
    <ConfigurationVersion majorVersion="4" minorVersion="0"/>
    <DisplayName>SF-SAML-Extractor</DisplayName>
    <Description>
        Reads a SAML assertion from a request header (base64-encoded or
        raw XML) and exposes it as saml.assertion.xml. Each calling
        proxy then runs its own native ExtractVariables policy against
        that variable with its own XPath (local-name()-based) to pull
        out whatever elements it needs.
    </Description>
    <Policies>
        <Policy>JS-Decode-SAML-Assertion</Policy>
    </Policies>
    <SharedFlows>
        <SharedFlow>default</SharedFlow>
    </SharedFlows>
</SharedFlowBundle>
