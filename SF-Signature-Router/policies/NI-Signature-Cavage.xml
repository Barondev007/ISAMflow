<?xml version="1.0" encoding="UTF-8"?>
<!--
    Placeholder for the HTTP Signature (Cavage /
    draft-cavage-http-signatures) sub-step.

    This policy stands in for the Step wired up in
    sharedflows/default.xml under Condition signature.type = "cavage".
    Replace this single policy with the real chain (e.g. AssignMessage
    to build the signing string from the request headers, ServiceCallout
    to the signing API, AssignMessage to set the Signature/Authorization
    header) — or add more <Step> entries with the same Condition to the
    flow if the sub-step needs several policies. Do not change the
    Condition.
-->
<RaiseFault name="NI-Signature-Cavage">
    <DisplayName>NI-Signature-Cavage</DisplayName>
    <IgnoreUnresolvedVariables>true</IgnoreUnresolvedVariables>
    <FaultResponse>
        <Set>
            <StatusCode>501</StatusCode>
            <ReasonPhrase>Not Implemented</ReasonPhrase>
            <Payload contentType="application/json">{
    "error": "not_implemented",
    "message": "HTTP (Cavage) signature sub-step is not implemented yet."
}</Payload>
        </Set>
    </FaultResponse>
</RaiseFault>
