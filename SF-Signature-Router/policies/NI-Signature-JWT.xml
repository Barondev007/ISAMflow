<?xml version="1.0" encoding="UTF-8"?>
<!--
    Placeholder for the JWT signature sub-step.

    This policy stands in for the Step wired up in
    sharedflows/default.xml under Condition signature.type = "jwt".
    Replace this single policy with the real chain (e.g. AssignMessage
    to build the claim set, ServiceCallout to the signing API,
    AssignMessage to attach the resulting JWT to the message) — or add
    more <Step> entries with the same Condition to the flow if the
    sub-step needs several policies. Do not change the Condition.
-->
<RaiseFault name="NI-Signature-JWT">
    <DisplayName>NI-Signature-JWT</DisplayName>
    <IgnoreUnresolvedVariables>true</IgnoreUnresolvedVariables>
    <FaultResponse>
        <Set>
            <StatusCode>501</StatusCode>
            <ReasonPhrase>Not Implemented</ReasonPhrase>
            <Payload contentType="application/json">{
    "error": "not_implemented",
    "message": "JWT signature sub-step is not implemented yet."
}</Payload>
        </Set>
    </FaultResponse>
</RaiseFault>
