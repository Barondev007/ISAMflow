<AssignMessage name="AM-Set-SAML-Response-Header">
    <DisplayName>AM Set SAML Response Header</DisplayName>
    <Set>
        <Headers>
            <!-- Base64 payload: a real zip archive (single "assertion.xml" entry) when
                 compressSaml resolved true, otherwise a plain base64 of the assertion XML. -->
            <Header name="X-SAML-Assertion">{saml.assertion.output}</Header>
            <Header name="X-SAML-Assertion-Compressed">{saml.assertion.compressed}</Header>
            <!-- saml.subject.id/notBefore/notOnOrAfter come from JC-Validate-SAML-Signature,
                 which parses the assertion XML itself rather than relying on XPath against
                 the outer ISAM response; see EV-Parse-Primary-Response's comment. -->
            <Header name="X-SAML-Subject-Id">{saml.subject.id}</Header>
        </Headers>
    </Set>
    <AssignVariable>
        <Name>isam.exchanged.subject.id</Name>
        <Ref>saml.subject.id</Ref>
        <Value></Value>
    </AssignVariable>
    <AssignVariable>
        <Name>isam.exchanged.assertion.notBefore</Name>
        <Ref>saml.assertion.notBefore</Ref>
        <Value></Value>
    </AssignVariable>
    <AssignVariable>
        <Name>isam.exchanged.assertion.notOnOrAfter</Name>
        <Ref>saml.assertion.notOnOrAfter</Ref>
        <Value></Value>
    </AssignVariable>
    <IgnoreUnresolvedVariables>true</IgnoreUnresolvedVariables>
    <AssignTo createNew="false" transport="http" type="request"/>
</AssignMessage>
